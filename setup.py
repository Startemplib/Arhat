from setuptools import setup, find_packages

setup(
    name="arpy",
    version="1.0",
    description="edited by myself",
    author="SYST",
    packages=find_packages(),  # 这会找到包含 __init__.py 的所有包
)
